version: '3.8'

services:
  db:
    image: postgres:latest
    environment:
      POSTGRES_USER: binam
      POSTGRES_PASSWORD: Bhandari
      POSTGRES_DB: binam
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U binam"]
      interval: 10s
      timeout: 5s
      retries: 5
    volumes:
      - ./docker-entrypoint-initdb.d:/docker-entrypoint-initdb.d


  minio-server:
    image: minio/minio
    ports:
      - "9000:9001"
    environment:
      MINIO_ROOT_USER: "JJiBrYDyIGUpRlXpTl00"
      MINIO_ROOT_PASSWORD: "lqD6aBXF5C793HQgOtZuQKlUwB5R5Z95FcLQFmU4"
    command: server /data
    volumes:
      - ./minio-data:/data

  app:
    build: .
    environment:
      DB_HOST: db
      DB_PORT: 5432
      DB_USER: binam
      DB_PASSWORD: Bhandari
      DB_NAME: binam
      MINIO_ENDPOINT: "minio-server:9000"
      MINIO_ACCESS_KEY: "GTug13wZkyKMp8jygOmI"
      MINIO_SECRET_KEY: "3I7Wtl43tuNCMw8UVLrMlzl2Y1ikIIdbnUkQYhcH"
      MINIO_SSL: "false"
    ports:
      - "8080:8080"
